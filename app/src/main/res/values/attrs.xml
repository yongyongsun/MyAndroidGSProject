<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>
    <declare-styleable name="item_view">
        <attr name="show_bottomline" format="boolean" />
        <attr name="show_leftimg" format="integer" />
        <attr name="show_text" format="string" />
    </declare-styleable>
    <declare-styleable name="layout_sub_item">
        <attr name="show_label" format="string" />
        <attr name="input_type" format="string" />
    </declare-styleable>

    <!-- 实现了 ListView 的上拉加载的 SwipeRefreshLayouot. -->
    <declare-styleable name="SwipeFlushView">
        <!-- 是否允许 下拉刷新. -->
        <attr name="flushingEnable" format="boolean" />
        <!-- 是否允许 上拉加载. -->
        <attr name="loadingEnable" format="boolean" />
        <!-- 是否需要 自动刷新. -->
        <attr name="autoFlushing" format="boolean" />
        <!-- 上拉加载的模式，上滑到底部后 是否 直接加载. -->
        <attr name="autoLoading" format="boolean" />
    </declare-styleable>

</resources>